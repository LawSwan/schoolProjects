```mermaid
flowchart LR
    A[Customer Service Assistant] --> U1((Handle Goods Return))
    U1 -. "<<include>>" .-> U2((Find Member Record))
    U1 -. "<<include>>" .-> U3((Check Order Status))
```mermaid
flowchart LR
    A[Customer Service Assistant] --> U1((Handle Goods Return))
    U1 -. "<<include>>" .-> U2((Find Member Record))
    U1 -. "<<include>>" .-> U3((Check Order Status))
```
```mermaid
flowchart LR
    A[Customer Service Assistant] --> U1((Handle Goods Return))
    B[Order Processing Clerk] --> U3((Check Order Status))

    U1 -. "<<include>>" .-> U2((Find Member Record))
    U1 -. "<<include>>" .-> U3((Check Order Status))

```mermaid
flowchart LR
    A[Customer Service Assistant] --> U1((Handle Goods Return))
    B[Order Processing Clerk] --> U3((Check Order Status))

    U1 -. "<<include>>" .-> U2((Find Member Record))
    U1 -. "<<include>>" .-> U3((Check Order Status))
`
```mermaid
flowchart LR
    A[Customer Service Assistant] --> U1((Handle Goods Return))
    B[Order Processing Clerk] --> U3((Check Order Status))

    U1 -. "<<include>>" .-> U2((Find Member Record))
    U1 -. "<<include>>" .-> U3((Check Order Status))
``
```mermaid
flowchart LR
    A[Customer Service Assistant] --> U1((Handle Goods Return))
    B[Order Processing Clerk] --> U3((Check Order Status))

    U1 -. "<<include>>" .-> U2((Find Member Record))
    U1 -. "<<include>>" .-> U3((Check Order Status))
```
```