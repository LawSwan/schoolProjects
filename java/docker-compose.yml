version: '3.8'

services:
  java-dev:
    build: .
    container_name: java
    volumes:
      - .:/app
    working_dir: /app
    stdin_open: true
    tty: true
    ports:
      - "8080:8080"
    command: tail -f /dev/null