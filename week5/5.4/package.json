{
  "name": "census-mean-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "client": "npm --prefix client run start --",
    "server": "npm --prefix server run dev",
    "build": "npm --prefix client run build",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
